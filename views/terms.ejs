<!DOCTYPE html>
<html lang="<%= currentLang %>" dir="<%= dir %>">
<%- include('partials/head', { pageTitle: t.terms_page.title + ' - ' + t.title }) %>
<body>
    <%- include('partials/navbar') %>

    <div class="container">
        <main class="content-page">
            <h2><%= t.terms_page.title %></h2>
            <p class="last-updated"><%= t.terms_page.last_updated %></p>
            
            <section>
                <h3><%= t.terms_page.intro_title %></h3>
                <p><%= t.terms_page.intro_text %></p>
            </section>

            <section>
                <h3><%= t.terms_page.usage_title %></h3>
                <p><%= t.terms_page.usage_text %></p>
            </section>

            <section>
                <h3><%= t.terms_page.disclaimer_title %></h3>
                <p><%= t.terms_page.disclaimer_text %></p>
            </section>
            
            <a href="/" class="back-link"><i class="fas fa-arrow-left"></i> <%= t.error.back_home %></a>
        </main>
    </div>

    <%- include('partials/footer') %>
</body>
</html>